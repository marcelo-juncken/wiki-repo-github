import React from 'react';
import {ButtonContainer} from "./styles";

const Button = () => {
    return (
        <ButtonContainer>
            Search
        </ButtonContainer>
    );
};

export default Button;